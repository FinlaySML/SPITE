#include "pch.h"
#include "Camera.h"
namespace Spite {
	Camera::Camera(glm::vec2 position, glm::vec2 dimensions, float rotation) : position{position}, dimensions{dimensions}, rotation{rotation} {
	}
}